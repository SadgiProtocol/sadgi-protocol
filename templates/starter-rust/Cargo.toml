[package]
name = "starter-rust"
version = "0.1.0"
edition = "2021"

[dependencies]
risc0-zkvm = { version = "0.20", default-features = false, features = ["std"] }
serde = { version = "1.0", features = ["derive"] }

[profile.release]
lto = true
opt-level = 3
panic = "abort"

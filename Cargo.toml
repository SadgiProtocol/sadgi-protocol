[workspace]
resolver = "2"

members = [
    "core/*",
    "contracts/*",
    "sdk/*"
]

exclude = [
    "templates/starter-soroban",
    "prover",
    "scripts"
]

[workspace.dependencies]
soroban-sdk = "20.0.0"
serde = { version = "1.0", features = ["derive"] }

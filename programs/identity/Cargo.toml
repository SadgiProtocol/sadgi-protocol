[package]
name = "sadgi-identity-program"
version = "0.1.0"
edition = "2021"
description = "Canonical Identity Reference Program for Sadgi Protocol"

[dependencies]
risc0-zkvm = { workspace = true }
sadgi-types = { path = "../../core/sadgi-types" }
soroban-sdk = { workspace = true }

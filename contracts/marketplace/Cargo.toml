[package]
name = "sadgi-marketplace"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
sadgi-types = { path = "../../core/sadgi-types" }
soroban-sdk = { version = "22.0.11" }

[dev_dependencies]
soroban-sdk = { version = "22.0.11", features = ["testutils"] }
